create table cursos(
    id int primary key,
	nome varchar,
	area      varchar,
	professor varchar
);

INSERT INTO cursos(nome, area, professor) VALUES
('Primeiro curso', 'Primeira area de curso','Primeiro professor do curso');